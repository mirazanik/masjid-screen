# Static Vite PWA for Cloudflare Workers & Pages builds
name = "masjid-screen"
compatibility_date = "2025-01-01"

# Required when Cloudflare runs: npx wrangler deploy
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
